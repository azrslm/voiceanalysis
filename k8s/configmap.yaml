apiVersion: v1
kind: ConfigMap
metadata:
  name: voice-quality-config
data:
  AWS_REGION: "ap-southeast-1"
  S3_BUCKET_NAME: "genai-aws-poc"
  BEDROCK_MODEL_ID: "anthropic.claude-3-5-sonnet-20240620-v1:0"
  # Note: AWS_PROFILE not needed when using IRSA
  # The service account will provide AWS credentials automatically
